---
- hosts: database
  become: true

  roles:
    - { role: docker, tags: [ 'datatabase' ] }

- hosts: webserver
  become: true
  gather_facts: false

  roles:
    - { roles: docker, tags: [ 'webserver' ] }

- hosts: loadbalancer
  become: true
  gather_facts: false

  roles:
    - { roles: docker, tags: [ 'loadbalancer' ] }


